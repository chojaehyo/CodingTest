#include <cstdio>
#include <cstring>
#include <algorithm>
using namespace std;

int main(){
  char arr[15] = {0};
  
  scanf("%s", arr);

  int len = strlen(arr);
  
  sort(arr, arr+len, greater<char>());
  
  for(int i=0;i<len;i++) printf("%c", arr[i]);
  
  return 0;
}
